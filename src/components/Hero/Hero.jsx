// Configurable height, parallax, tint/filter
// Can have child text
// Children can animate up

// Will need state to use lifecycle methods for the animation
// Libraries
import React from 'react';

// Components

// Styles
import heroStyles from './Hero.module.scss';

export default () => (
    <div className={heroStyles.hero}>I am a hero</div>
)